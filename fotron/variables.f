!gfortran

      program testingVariables
        implicit none
        !print*, "Helllo"

        ! ----- integer
        !integer :: largeval
        !print*, huge(largeval)

        ! ---- real
        ! real :: p, q, res
        ! p = 2.0
        ! q = 3.0
        ! res = p/q
        ! print*, res

        ! ---- character 
        character (len = 40 ) :: name
        name = "Miyuru CG"
        print*, name
        
    
      end program testingVariables